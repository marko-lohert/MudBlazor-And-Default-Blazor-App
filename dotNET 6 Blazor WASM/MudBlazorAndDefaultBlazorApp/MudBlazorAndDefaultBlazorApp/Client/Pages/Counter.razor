@page "/counter"

<PageTitle>Counter</PageTitle>

<MudText Typo="Typo.h3">Counter</MudText>

<MudText Typo="Typo.body1">Current count: @currentCount</MudText>

<MudButton Variant="Variant.Filled" Color="Color.Primary" Disabled="false" @onclick="IncrementCount">Click me</MudButton>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
